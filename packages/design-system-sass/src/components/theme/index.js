import React from 'react';
import '../../styles/output.css';

export const H1 = (props) => <h1 className="ds-h1-heading">{props.children}</h1>;

export const H2 = (props) => <h2 className="ds-h2-heading">{props.children}</h2>;

export const P = (props) => <p className="ds-paragraph-text">{props.children}</p>;

